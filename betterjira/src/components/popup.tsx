"use client";

import React, { useState, useRef, forwardRef, useImperativeHandle } from "react";

const FileUpload = forwardRef(({ }, ref) => {

  return (
    <label>

    </label>
  );
});

FileUpload.displayName = "FileUpload";

export default FileUpload;
